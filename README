1.Установить зависимости pip install requirements.txt
2.Запустить app.py
3.Проверить через jn test_api.ipynb

В данном проекте создаются две таблицы, Cars и Dilers,
к которым можно образаться по API(с поддержкой CRUD).
Все данные, в том числе из базы данных, проверяются через пакет валидации pydantic.


Dilers содержит информацию о дилерах
"id" : идентификатор
"name" : наименование дилера
"address" : адрес местанахождения(в примерах указан не полный)

Cars хранит в себе параметры об автомобилях, и имеет зависимисть с Dilers через
атрибут dilers_id(Dilers.id)
"id" : идентификатор
"name" : наименование авто
"sold" : статус авто(False - не продан, True - продан)
"year_of_manufacture" : год выпуска
"color" : цвет
"broken" : статус авто(False - не сломан, True - сломан)
"price" :цена
"num_owners" : число владельцев
"diler_id" : зависимисть с Dilers через атрибут dilers_id(Dilers.id)

Для получения всей информации из каждой таблицы, необходимо обратиться
соответсвующим образом:

    GET /<dilers or cars>

Для создания записи:
    POST /<dilers or cars>

Для получения конкретной информации, следует обратиться к следующему запросу
    /custom_<diler or car>/<val>

Пример:
    /custom_diler/id=1 , Отобразит все записи, у которых дилер имеет id=1
    /custom_diler/color=silver , Отобразит все записи, у которых авто имеет цвет silver

Для изменения конкретной информации по id, следует обратиться к следующему запросу
    PUT /custom_<diler or car>/<int:id>" - изменение
    DELETE /custom_<diler or car>/<int:id>" - удаление